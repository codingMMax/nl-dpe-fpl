# VTR scratch: keep only the parsed VPR log per run; everything else is
# regenerable via run_vtr_softmax.py (blif/net/place/route are 10-100MB).
vtr_*_input/
vtr_*_seed*/*
!vtr_*_seed*/vpr_stdout.log
# smoke work dirs are regenerated by run_softmax_smoke.py (seeded RNG)
smoke_*/
